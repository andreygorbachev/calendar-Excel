# calendar-Excel

We want to access the calendar library from Excel.

For things to work the calendar-Excel.dll should be somewhere in PATH and calendar.xlam should be included in Excel's Add-ins.

The following functions are currently implemented:
1) CalendarSetAsOfDate
2) CalendarIsBusinessDay
3) CalendarCountBusinessDays
(which are holefully self explanatory)
